<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Name="DownloadsPageRoot"
    x:Class="LSMA.Pages.DownloadsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LSMA.Pages">

    <Grid Padding="30,16,30,24" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Spacing="8">
                <TextBox Text="{Binding OnlineQuery, Mode=TwoWay}" PlaceholderText="搜索模组" Width="220" />
                <Button Content="搜索" Command="{Binding SearchOnlineCommand}" Style="{StaticResource SubtleButtonStyle}" />
                <ComboBox ItemsSource="{Binding OnlineSortOptions}" SelectedItem="{Binding SelectedOnlineSort, Mode=TwoWay}" MinWidth="140" PlaceholderText="排序" />
                <Button Content="刷新" Command="{Binding RefreshCommand}" Style="{StaticResource SubtleButtonStyle}" />
            </StackPanel>
            <TextBlock Grid.Column="1" Text="{Binding OnlinePanelTitle}" Style="{StaticResource SectionTitleStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" />
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="380" />
            </Grid.ColumnDefinitions>

            <!-- Mod list -->
            <Border Background="Transparent" BorderThickness="0" Padding="4,0,0,0">
                <StackPanel>
                <ListView ItemsSource="{Binding OnlineMods}" SelectedItem="{Binding SelectedOnlineMod, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{ThemeResource CardBackgroundBrush}" BorderBrush="{ThemeResource CardBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,3,8,3">
                                <Grid ColumnSpacing="8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="3">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Author}" Style="{StaticResource MutedTextStyle}" />
                                    <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource AccentBrush}" />
                                </StackPanel>
                                <Button Grid.Column="1" Content="Nexus" VerticalAlignment="Center"
                                        Tag="{Binding}"
                                        Click="NexusButton_Click"
                                        Style="{StaticResource SubtleButtonStyle}" />
                            </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <Button Content="加载更多" Command="{Binding LoadMoreCommand}"
                        Style="{StaticResource SubtleButtonStyle}"
                        HorizontalAlignment="Center" Margin="0,8,8,4"
                        Visibility="{Binding LoadMoreVisibility}" />
                </StackPanel>
            </Border>

            <!-- Detail panel -->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                <ScrollViewer>
                    <StackPanel Spacing="12">
                        <TextBlock Text="{Binding SelectedOnlineMod.Name}" Style="{StaticResource SectionTitleStyle}" />
                        <TextBlock Text="{Binding SelectedOnlineMod.Summary}" Style="{StaticResource BodyTextStyle}" />

                        <StackPanel Orientation="Horizontal" Spacing="7">
                            <ToggleButton Content="{Binding FavoriteButtonText}" Command="{Binding ToggleFavoriteCommand}"
                                          IsChecked="{Binding SelectedOnlineMod.IsFavorite, Mode=OneWay}"
                                          Style="{StaticResource ChoiceToggleStyle}" />
                        </StackPanel>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <ComboBox ItemsSource="{Binding OnlineFiles}" SelectedItem="{Binding SelectedOnlineFile, Mode=TwoWay}"
                                      DisplayMemberPath="DisplayText" PlaceholderText="选择文件" MinHeight="32" />
                            <Button Grid.Column="1" Content="刷新" Command="{Binding LoadFilesCommand}"
                                    Style="{StaticResource SubtleButtonStyle}" MinHeight="32" Padding="12,0" />
                        </Grid>

                        <Button Content="下载并预检" Command="{Binding DownloadFileCommand}" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" />
                        <Button Content="取消下载" Command="{Binding CancelDownloadCommand}" Style="{StaticResource NeutralButtonStyle}" HorizontalAlignment="Left" />
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>
